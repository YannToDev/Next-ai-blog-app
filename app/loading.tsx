
// --- C'est le composant rendu par défaut lorsque des données sont en chargement pour la page principale ---
// NOTE: Pour fonctionner il doit être obligatoirement à la racine du dossier App et s'appeler loading.tsx

import React from 'react'

function loading() {
  return (
    <div>loading</div>
  )
}

export default loading